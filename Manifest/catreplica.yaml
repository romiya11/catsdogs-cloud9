apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicas
  labels:
    app: cats
spec:
  replicas: 3             
  selector:               
    matchLabels:
      app: cats
  template:           
    metadata:
      labels:
        app: cats    
    spec:
      containers:         
      - name: cats
        image: 054326382769.dkr.ecr.us-east-1.amazonaws.com/assignment2:cat
        ports:
       